{
  "name": "offlo-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
